<div class="show-container">
  <div class="row">
    <div class="col-12">
      <div id="container-bloc-text">
        <h1>Check out the planets</h1> <%= link_to 'add a new planet', new_planet_path, class: 'btn btn-warning' %>
      </div>
    </div>
    <% if @message.present? %>
      <div><%= @message %></div>
    <% end %>
  </div>
  <%= render 'shared/card' %>
</div>
